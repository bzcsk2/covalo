/**
 * OpenTUI 入口
 * TUI-OT-00: 占位实现，待后续任务填充
 */

export interface OpenTUIStartOptions {
  // 后续填充 engine 等参数
}

export async function startOpenTUI(_options: OpenTUIStartOptions): Promise<void> {
  // eslint-disable-next-line no-console
  console.log('[tui-opentui] OpenTUI mode selected (DEEPREEF_TUI=opentui). Implementation pending.')
  // TODO: 实现 OpenTUI 渲染循环
}